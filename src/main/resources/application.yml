spring:
  application:
    name: Pupfit
  datasource:
    url: jdbc:mysql://${DB_URL:localhost}:3306/${DB_NAME:pupfit}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:cho980625}
    driver-class-name: com.mysql.cj.jdbc.Driver

#    url: jdbc:mysql://localhost:3306/pupfit?serverTimezone=UTC&characterEncoding=UTF-8
#    username: root
#    password: cho980625
#    driver-class-name: com.mysql.cj.jdbc.Driver

    ### 아직 db 도메인이름을 해당 mysql의 컨테이너이름과 똑같이 해줘야하는 약간의 불편함이 존재한다. 해당 pupfit-database 부분을 동적으로 진행할수는없을까?
#    url: jdbc:mysql://pupfit-database:3306/pupfit
#    username: root
#    password: 1234
#    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true

  #logging.level:
    #org.hibernate.SQL: debug
    #org.apache.coyote.http11: debug

# 디폴트 경로 : http://localhost:8080/swagger-ui/index.html
springdoc:
  swagger-ui:
    groups-order: DESC
    tags-sorter: alpha
    operations-sorter: method
    disable-swagger-default-url: true
    display-request-duration: true
    defaultModelsExpandDepth: 2
    defaultModelExpandDepth: 2
  api-docs:
    path: /api-docs
  show-actuator: true
  default-consumes-media-type: application/json
  default-produces-media-type: application/json
  writer-with-default-pretty-printer: true
  model-and-view-allowed: true
  paths-to-match:
    - /api/**